# Copy to .env and customize if needed.
# Default uses local SQLite file (globalevents.db) in backend/ folder.
# For PostgreSQL later, you can switch DATABASE_URL to:
# DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@localhost:5432/globalevents
# SECRET_KEY=your-secret-key-for-jwt
